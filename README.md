## Vertex AI Environment

Set the following env vars for Vertex AI authentication:

- `GOOGLE_APPLICATION_CREDENTIALS` (path to service account JSON)
- `GOOGLE_CLOUD_PROJECT`
- `GOOGLE_CLOUD_LOCATION` (e.g. `us-central1`)
